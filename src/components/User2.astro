---
const response = await fetch("https://rickandmortyapi.com/api/character");
const data = await response.json();

const personaje = data.results[2];
---

<div
  class="text-white rounded-lg border shadow-md bg-gray-800 border-gray-700
hover:scale-105 hover:bg-gray-700 hover:border-gray-500 transation duration-300 delay-150 md:delay-300 flex
flex-col"
>
  <h1 class="text-center">Personaje</h1>
  <h2 class="text-center">{personaje.name}</h2>
  <img src={personaje.image} alt="" />
  <p>Estado: {personaje.status}</p>
  <p>Especie: {personaje.species}</p>
  <p>Planeta: {personaje.origin.name}</p>
</div>
